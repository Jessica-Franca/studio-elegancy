---
description: Este repositório Git é a única fonte oficial do Studio Elegancy.
alwaysApply: true
---

# Fonte oficial

O diretório raiz deste workspace é o repositório oficial `Jessica-Franca/studio-elegancy`.

O aplicativo web é a raiz do repositório (`index.html`, `css/`, `js/`).

Não criar clones, cópias ou versões alternativas do projeto.

Não trabalhar em diretórios temporários.

Não utilizar projetos legados como fonte de implementação.

Todo novo desenvolvimento deve ocorrer neste workspace.

Antes de alterações importantes, verificar o Git root e `git status`.

Depois de testar, revisar o diff e versionar no Git.

Não alterar legado Python/PySide6/SQLite sem instrução explícita.

Não commitar `DocsCliente/`, `.env`, credenciais, `Auditorias/` internas ou dados reais de clientes.
